@startuml
(*) --> "Администратор входит в систему"

if "Администратор авторизован" then
  --> "Система отображает панель администратора"

  if "Администратор выбирает управление продуктами" then
    --> "Система отображает список текущих продуктов"

    if "Администратор решает добавить новый продукт" then
      --> "Система предоставляет форму для добавления продукта"

      if "Администратор вводит данные для нового продукта" then
        --> "Система проверяет корректность введенных данных"

        if "Данные корректны" then
          --> "Система добавляет новый продукт"
          --> "Система отображает подтверждение добавления"
          --> (*)
        else
          --> "Система отображает сообщение об ошибке ввода данных"
          --> (*)
        endif
      else
        --> "Администратор отменяет добавление продукта"
        --> (*)
      endif
    else
      --> "Администратор выбирает существующий продукт для изменения"

      if "Администратор вводит новые данные для продукта" then
        --> "Система проверяет корректность введенных данных"

        if "Данные корректны" then
          --> "Система сохраняет измененные данные продукта"
          --> "Система отображает подтверждение изменений"
          --> (*)
        else
          --> "Система отображает сообщение об ошибке ввода данных"
          --> (*)
        endif
      else
        --> "Администратор отменяет изменения для выбранного продукта"
        --> (*)
      endif
    endif
  else
    --> "Администратор продолжает другие операции в панели администратора"
    --> (*)
  endif
else
  --> "Администратор не авторизован"
  --> (*)
endif
@enduml
